import { colors } from "./Colors";
import { space, lineHeights } from "./Spacing";
import { sizes, sizesBorder } from "./Sizes";
import { fonts, fontWeights, fontSizes } from "./Fonts";

export const theme = {
  colors,
  space,
  sizesBorder,
  lineHeights,
  sizes,
  fonts,
  fontSizes,
  fontWeights,
};
